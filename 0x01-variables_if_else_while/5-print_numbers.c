#include <stdio.h>
/**
 * main -  a program that prints all single digit
 *
 * Return: Always 0 (success/correct)
 */
int main(void)
{
int d;

for (d = 0 ; d < 10; d++)
{
printf("%d", d);
}
printf("\n");
return (0);
}
